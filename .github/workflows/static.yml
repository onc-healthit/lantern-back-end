name: Static Analysis

on: [push]

jobs:
  lintrs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Lintrs
        id: lintrs
        run: go get github.com/golangci/golangci-lint/cmd/golangci-lint && make lint
